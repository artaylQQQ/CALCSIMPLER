---
title: "Loan Payment (PMT) Calculator"
description: "Estimate your fixed monthly loan payment."
slug: "loan-payment-calculator"
cluster: "Finance & Loans"
datePublished: "2025-08-16"
---

import BaseLayout from "../../src/layouts/BaseLayout.astro";
import Calculator from "../../src/components/Calculator.astro";

export const schema = {
  "slug":"loan-payment-calculator",
  "title":"Loan Payment (PMT) Calculator",
  "locale":"en",
  "inputs":[
    {"name":"principal","type":"number","min":0,"step":0.01,"hint":"Loan amount (PV)"},
    {"name":"rate","type":"number","min":0,"step":0.0001,"hint":"Annual interest rate (%)"},
    {"name":"months","type":"number","min":1,"step":1,"hint":"Number of months"}
  ],
  "formula_js":"(inputs) => { const r = (inputs.rate/100)/12; const n = inputs.months; const pv = inputs.principal; const pmt = r===0 ? pv/n : (r*pv)/(1-Math.pow(1+r,-n)); return { result: Number(pmt.toFixed(2)), steps: [`Monthly rate r = ${(inputs.rate/100)/12}`, `Payment = r*PV / (1-(1+r)^-n)`] }; }",
  "units":{"input":"USD","output":"USD/month"},
  "intro":"Estimate your fixed monthly loan payment using the standard annuity formula.",
  "examples":[{"input":{"principal":10000,"rate":6,"months":36},"output":"304.22"}],
  "faqs":[{"q":"What formula do you use?","a":"We use PMT = r×PV / (1−(1+r)^(−n)) with a monthly rate."}],
  "related": ["simple-interest-calculator","compound-interest-calculator","npv-calculator"],
  "disclaimer":"Educational information, not professional advice.",
  "schema_org":"FAQPage|SoftwareApplication"
};

<BaseLayout title={schema.title} description={schema.intro}>
  <Calculator schema={schema} />
</BaseLayout>
